#include <stdio.h>

/** \brief Calcular el factorial de un numero entero
 *
 * \param operador int el numero a ser factoreado en la funcion
 * \return int el resultado del factoreo
 *
 */
int CalcularFactorial(int operador);
